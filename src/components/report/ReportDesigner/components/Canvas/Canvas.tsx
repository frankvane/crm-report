// 画布主组件

export default function Canvas() {
  return (
    <div
      style={{
        padding: 16,
        fontWeight: 600,
        color: "#1976d2",
        background: "#e3f2fd",
        border: "2px solid #1976d2",
        borderRadius: 6,
      }}
    >
      画布区（静态内容）
    </div>
  );
}
